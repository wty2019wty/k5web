# npm 全局包安装路径（项目内）
prefix=./.local/npm
# npm 缓存路径（项目内）
cache=./.local/npm/cache
# npm 配置文件路径（项目内，不生成系统 .npmrc）
userconfig=./.local/npm/.npmrc
# 可选：国内镜像（加速下载，不影响隔离）
registry=https://registry.npmmirror.com